<?php

/**
 * Create and return the application configuration here. The returned configuration
 * must be an instance of `\Magpie\Configurations\AppConfig` (or it's subclasses),
 * otherwise will be a fatal error.
 */

use App\Configurations\AppConfig;

return new AppConfig();